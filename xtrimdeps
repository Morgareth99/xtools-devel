#!/bin/sh
for deptype in hostmakedepends makedepends depends checkdepends; do
	pkgs="$(xmindep $(xgetdeps "$deptype" "$1"))"
	[ -z "$pkgs" ] && continue
	printf "%s=\"" "$deptype"
	for pkg in $pkgs ; do
		printf "%s " "$pkg"
	done | sed -e 's/[[:space:]]*$/"\n/g'
done
